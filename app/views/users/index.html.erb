<h1>ユーザー一覧</h1>

<table>
  <thead>
    <tr>
      <th>メールアドレス</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= link_to '詳細', user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @users %>

<br>

<%= link_to '書籍一覧', books_path %>
